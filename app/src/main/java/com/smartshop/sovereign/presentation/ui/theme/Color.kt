package com.smartshop.sovereign.presentation.ui.theme

import androidx.compose.ui.graphics.Color

// Noir-Cyber Theme Colors
object SmartShopColors {
    // Primary colors
    val Black = Color(0xFF000000)
    val ElectricBlue = Color(0xFF0070FF)
    
    // Background colors
    val DarkBackground = Color(0xFF0A0A0A)
    val CardBackground = Color(0xFF1A1A1A)
    val SurfaceVariant = Color(0xFF2A2A2A)
    
    // Text colors
    val TextPrimary = Color(0xFFFFFFFF)
    val TextSecondary = Color(0xFFB0B0B0)
    val TextMuted = Color(0xFF707070)
    
    // Accent colors
    val SuccessGreen = Color(0xFF00C853)
    val ErrorRed = Color(0xFFFF1744)
    val WarningOrange = Color(0xFFFF9100)
    
    // Overlay colors
    val Overlay = Color(0x80000000)
    val BlueOverlay = Color(0x330070FF)
}

val SmartShopTypography = androidx.compose.ui.text.TextStyle(
    // Will be defined in theme
)